#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 48;
	int k = 97;

		while (n <= 57)
		{
			putchar(n);
			n++;
		}
		while (k <= 102)
		{
			putchar(k);
			k++;
		}
		putchar('\n');
		return (0);
}
